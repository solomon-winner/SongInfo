import { createSlice } from "@reduxjs/toolkit";

export const statSlice = createSlice({
    name: 'stat',
    initialState: {
        totalSong: 0,
        artists: 0,
        albums: 0,
        genres: 0,
    },
    reducers : {
        
    }
}) 